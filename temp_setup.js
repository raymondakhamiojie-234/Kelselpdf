const mysql = require('mysql2/promise'); require('dotenv').config(); async function run() { const pool = mysql.createPool({ host: process.env.DB_HOST, port: process.env.DB_PORT, user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME, ssl: { rejectUnauthorized: false } }); await pool.query('CREATE TABLE IF NOT EXISTS academy_categories (id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, name VARCHAR(100) NOT NULL, slug VARCHAR(100) NOT NULL UNIQUE, icon VARCHAR(100), created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP)'); await pool.query('CREATE TABLE IF NOT EXISTS academy_courses (id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, category_id INT(6) UNSIGNED, title VARCHAR(200) NOT NULL, slug VARCHAR(200) NOT NULL UNIQUE, description TEXT, instructor VARCHAR(100), duration_hours DECIMAL(5,2), skill_level VARCHAR(20), thumbnail VARCHAR(255), is_premium BOOLEAN DEFAULT TRUE, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP, FOREIGN KEY (category_id) REFERENCES academy_categories(id) ON DELETE CASCADE)'); await pool.query('CREATE TABLE IF NOT EXISTS academy_modules (id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, course_id INT(6) UNSIGNED, title VARCHAR(200) NOT NULL, order_index INT(3), FOREIGN KEY (course_id) REFERENCES academy_courses(id) ON DELETE CASCADE)'); await pool.query('CREATE TABLE IF NOT EXISTS academy_lessons (id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, module_id INT(6) UNSIGNED, title VARCHAR(200) NOT NULL, type ENUM(\'video\', \'reading\', \'assignment\', \'quiz\') NOT NULL, content TEXT, video_url VARCHAR(255), duration_minutes INT(3), order_index INT(3), FOREIGN KEY (module_id) REFERENCES academy_modules(id) ON DELETE CASCADE)'); await pool.query('CREATE TABLE IF NOT EXISTS academy_progress (id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY, user_id INT(6) UNSIGNED, lesson_id INT(6) UNSIGNED, completed BOOLEAN DEFAULT FALSE, completed_at TIMESTAMP NULL, FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE, FOREIGN KEY (lesson_id) REFERENCES academy_lessons(id) ON DELETE CASCADE, UNIQUE KEY user_lesson (user_id, lesson_id))'); console.log('Tables created!'); process.exit(0); } run();
